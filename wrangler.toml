name = "chalee-rag-agent"
main = "worker/src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# ============ Workers AI ============
[ai]
binding = "AI"

# ============ Vectorize 向量数据库 ============
[[vectorize]]
binding = "VECTORIZE"
index_name = "rag-documents"

# ============ R2 文件存储 ============
[[r2_buckets]]
binding = "R2"
bucket_name = "chalee-rag-uploads"

# ============ KV 缓存 ============
[[kv_namespaces]]
binding = "KV"
id = "c1861ad3b977476f9a7df0c92b78b4be"

# ============ D1 元数据库 ============
[[d1_databases]]
binding = "DB"
database_name = "rag-metadata"
database_id = "639f9576-3e8c-4c65-97ce-439ddf24a70c"

# ============ 环境变量 ============
[vars]
CHUNK_SIZE = "800"
CHUNK_OVERLAP = "100"
TOP_K_RESULTS = "5"
MAX_TOKENS = "800"

# ============ 可选: 使用 OpenAI 替代 Workers AI ============
# 在 wrangler secret put OPENAI_API_KEY 中配置
# [vars]
# USE_OPENAI = "true"

# ============ 本地开发 ============
[dev]
port = 8787
local_protocol = "http"
